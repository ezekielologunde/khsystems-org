# Legacy Export (Reference Only)

This folder contains the original flat HTML export of the old King Health Systems, Inc. WordPress site (theme "kinghealth"). It is **not served by the new Next.js app** — it exists purely as a content reference for anyone verifying copy migrated into `src/lib/content/*.ts`.

Safe to delete once content migration has been fully verified against the live rebuilt site.
